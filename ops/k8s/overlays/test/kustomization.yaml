apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: l4h

resources:
  - ../../base

namePrefix: ""
nameSuffix: ""

commonLabels:
  environment: test
  managed-by: kustomize

# TEST-specific patches
patchesStrategicMerge:
  - ingress-patch.yaml

# TEST environment uses standard resource names
# No special suffix needed as this is the default environment

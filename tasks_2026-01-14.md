# Task List - January 14, 2026

## CannLaw

1.  **Attorney Images**: 
    -   Identify the 3 attorneys with missing image files.
    -   Do not pre-seed these attorneys.
    -   Ensure database entries for files only exist if the physical file exists in the images directory.
2.  **Contact Page**: 
    -   Fix 400 Bad Request/Validation errors when creating a message.
    -   Ensure messages are sent to all admin users.
3.  **Social Media**: 
    -   Fix LINE button link (currently goes to Services page).
4.  **Dashboard Login**: 
    -   Fix 403 errors for `Activity` and `Stats` endpoints.
5.  **User Edit (Time Tracking)**: 
    -   Fix 400 Bad Request on `time-tracking/active`.
    -   Fix 400 Bad Request on `time-tracking/start`.
6.  **User Edit (Name Change)**: 
    -   Fix 404 error when changing a user's name (fails on `cases` and case number lookup).
7.  **Clients Page (Access)**: 
    -   Fix 403 Forbidden error for admins.
    -   Ensure admins can see **EVERY** client (optional: add "my cases" filter, but default to all).
8.  **Clients Page (UI)**: 
    -   Fix Dark Mode issues on the 4 search boxes in the filters section.
9.  **Sync Clients**: 
    -   Fix 403 Forbidden error (console) despite success popup.
10. **Billing Page**: 
    -   Fix Dark Mode issues (Notes, Recent Time Entries header, Date/Client bar).
    -   Fix 403 error on clients API.
    -   Fix 400 errors on time-tracking APIs (`active`, `entries`, `stats`).
    -   Verify if time-tracking is actually implemented.
11. **Messages (Recipients)**: 
    -   Fix 500 error on recipients endpoint.
    -   Fix LINQ `string.Format` translation failure in `DbSet<User>` query.
12. **Messages (Click)**: 
    -   Fix 405 Method Not Allowed error when clicking a message.
13. **Profile Picture**: 
    -   Fix 404 error on upload.
    -   Fix issue where "new" picture doesn't show on staff page (old one persists).
14. **Navigation**: 
    -   Fix Dark Mode for "Visa Library" and "Hello" dropdowns (white on white on hover).

## Law4Hire

1.  **Session/Versioning**: 
    -   Implement version check: New builds should refresh the cookie.
    -   If version mismatch, expire and delete cookie.
    -   Sync version logic with CannLaw.
2.  **Pricing Page (Consultations)**: 
    -   Fix duplicate initial consultations (162 vs 150).
    -   Ensure configuration is driven by the admin package page.
3.  **Appointments**: 
    -   Disable the "Appointments" button (keep visible, just disabled).
4.  **Messages**: 
    -   Fix 500 error for recipients (Same as CannLaw #11).
5.  **Admin Settings**: 
    -   Add Stripe configuration field (Law4Hire ONLY).
6.  **Settings Save**: 
    -   Investigate/Fix 401 error on save (cookie clearing workaround suggests auth issue).
7.  **Manage Users**: 
    -   Fix 401 fetching users.
    -   Add ability to edit Email, First Name, Last Name, Phone Number.
8.  **Case Management**: 
    -   Fix Review button failure (likely due to missing interview).
9.  **Pricing Page (Management)**: 
    -   Fix Dark Mode (bright white cards).
    -   Implement ability to edit prices and packages.
    -   Fix Visa Types count (showing 29, should be ~91: 88 identified + Foreign Adoption, Citizenship, Asylum).
    -   Ensure prices are database-driven including LLC prices.
10. **Package Page (Menu)**: 
    -   Restore "Packages" page to the menu.
11. **Package Page (Functionality)**: 
    -   Add "Remove Package" functionality.
    -   Remove "Package Code" field and validation (generate from Display Name).
12. **Interview Questions (Answer Path)**: 
    -   Fix White Page error (`Trash2 is not defined`).
    -   Fix Logic: Answer discrimination should happen at the ANSWER level, not the Question level.
    -   Show ALL Visa types for discrimination fields.
13. **USCIS Forms**: 
    -   Fix Delete Popup Dark Mode (black text on grey).
    -   Remove "LLC Fee" percentage field (should not be a percentage).
    -   Fix Mapping Dropdowns: Show specific Visa types (F-1, F-2, etc.), not just categories.
14. **Visa Library**: 
    -   Verify/Fix Visa count (should be ~91, currently 29).
15. **Attorney Management**: 
    -   Fix missing pictures issue.
    -   Fix Dark Mode text issues (black on dark).
    -   Fix 500 Internal Server Error on Create.
    -   Fix negative display order numbers.
16. **Reports and Analytics**: 
    -   Fix redirect to home page.

Changed corporate and primary phone numbers in System Settings. No change reflected throughout Cannlaw site. Not present on Law4Hire.
Changed Focus statement and the change was not reflected on Cannlaw site. Not present on Law4Hire.
Values for Social Media are hard-coded in Cannlaw (or otherwise derived elsewhere) but system configuration does not have proper pre-seeding nor does any change reflect on the site. 
The Unique selling points don't seem to be reflected anywhere on the site. 

(Must ask question: Do the values stored for the System Settings actually get used ANYWHERE?) 

Reports and Analytics button takes us to a screen that immediately notifies of failed to fetch. 

Editing the legal staff and testing if the changes are able to take effect does not work. I can edit, I can save. I have no idea if the text in the bio is reflected, because the staff page on Cannlaw says invalid response format.
Editing the picture for the attorney should NOT pop up the file selector tool. It should pop up a list, with preview, of the available pictures for a particular attorney. It should have the preview, the list of all available pictures, and three buttons. Button 1 should be "Select Picture" and whatever picture is selected (and the current picture should always be the default selected, and if no picutre is available, disable the button), a "Upload Picture" which will then provide the file selector tool with only image formats available, and the preview will be displayed when the picture is uploaded. The file will be saved in the EXACT SAME DIRECTORY as the other images. All images for a particular attorney MUST be noted in a table in the database so we can ALWAYS find ALL images associated with that attorney. The third button, again, only available if a picture is selected, is "Delete Picture". 

Adding a new attorney does not display the picture. This will be fixed hopefully with the previous piece. The files are not being uploaded to the images/attorneys directory, but instead the /attorneys directory, but cannot be pulled up. So, it creates a link, but not uploading the file. 
If you deactivate a profile, it should be noted as "deactivated", and shaded red or some other indicator, without edit, but a "Reactivate" button. 
I can't test the Law Library on Cannlaw as it's giving a Error loading resources: Unexpected token '<', "<!doctype "... is not valid JSON error, but the editing on the admin panel works for Law4Hire. 
The packages page is literally for the packages like doing the whole thing with guarantee and a lawyer, down to a paralegal only or even a self-filing with legal review. It is NOT for the H-1B or F-1 or any other Visa type. Do NOT pre-seed with errant packages. Remove any pre-seeding and remove any data from packages upon the rebuild that will give us that chaotic mess. Updating for the individual Visa types is LITERALLY the pricing page, not Package. 

On Cannlaw, when I try to send a consultation message, it gives me an error.
On Cannlaw, it's not using the database for the people we have registered. They're using the same user list. So, on Law4Hire, I should be able to see the user Abu@testing.com and that same user, with that same password, using the same data tables will be able to log in. I cannot log in as Dcann@cannlaw.com so I cannot test ANYTHING on the dashboard of Cannlaw.  


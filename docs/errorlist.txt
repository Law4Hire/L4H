CannLaw

1) In the Messages page, you STILL can't select a specific message. 
2) Somehow, we reverted. Now, if you hover over the Visa Library link in the dashboard or the Hello link in the dashboard, the text is now NOT properly colored. It now uses a white background, and that's not correct on a dark mode. 
3) When you change the picture in the profile page, you DON'T see a pop-up to see what pictures are already downloaded and you get the file selector. When you pick it, it says it's uploaded, but then gives a 404 error. When you go to the Attorney/Staff page, it shows the original picture and in the console, it gives a 404 error for the new picture. 
4) In the Billing page, it STILL doesn't filter or give a check box. I said put a check box for Admin users, but the DEFAULT MUST BE just the assigned cases. 
5) In the Billing page, it FINALLY saves the changes to the billing, but the dashboard for the billing page doesn't reflect the saved information. So, the summary at the bottom for hours and dollars stays the old value. If I exit out of the page, and re-enter, it works. If you start, get to a billed amount and save, it does update properly.
6) In the Settings, the dark mode is extremely off. Multiple white background boxes and multiple black text on dark background.
7) In the Attorneys/Staff page, the bio does not go to full screen with a click on the bio. 

Law4Hire

1) In the Packages page, when deleting a package, there's a 405 error on the console and it fails. 
2) On the Case Summary page, we need to not have Legal Professionals actually have a case assigned to them, so they shouldn't be showing up in the display.
3) I went to the Pricing Management screen and it says failed to load. 
4) I get the same on the Package management.
5) I have massive failures on the Messages page. 

It appears the server just isn't running properly. You have SSH access, please go forward and check the server logs and then do a local UI test to determine if this pages are giving errors. 

All of the remaining items on the list, I cannot verify as I am getting too many errors to continue testing. 

4) On the messages page, all the failures we saw on Cannlaw, are not there, except the 500 error for the recepients.
5) In the admin panel, we have the system settings (We're at the admin, and there were only 4 things on the rest of the site I found, so that means we're doing great on l4h), there's settings page and there needs to be a Stripe configuration for our payment system we're going to add to Cannlaw later. (DO NOT ADD TO CANNLAW YET).
6) In the settings, if I change the data, I get a 401 error on save. After I cleared the cookies, it went away.
7) In the Manage Users, I get a 401 fetching the users. I cleared the cookies and it went away. Also, I want to have the ability to change the user's email address or their first or last names, and their phone number from that page.
8) There's a case management page and in there, we have minimal amount of ability to edit. The review button fails to bring up the case, but I suspect that's due to no completed interview.
9) The pricing page, the dark mode is not working. All the cards are bright white. The ability to price the different Visa types with both the price (which is what is displayed) and the ability to EDIT those prices needs to be set up. If I click a card, it gives me a pop-up, with no ability to edit a package with that pricing, but does give me the ability to delete one and create one. Law4Hire is an LLC to provide services to the Cann Legal Group (cannlaw) and so, as such, we've put in to the database a point where we can have the LLC price, but that's not in the editing of pricing at all right now. It also has only 29 different Visa types. We've identified 88 and then also Foreign Adoption, Citizenship and Asylum claims. So, there should be 91 and there's only 29. These should be database driven and the database is supposed to have all those. If they're not all in the database, I need to know. This MUST be corrected.
10) The Package page has been removed from the menu. Not disabled. Removed from the menu. The page still exists. I need that back.
11) The Package page does not have an ability to remove a package. We REMOVED and somehow got re-added, the package code. This confuses the client and I don't need it there. Generate one based on the display name. When you've got that done, remove the validation for the code.
12) There's a new feature in the interview questions to allow you to add an answer path. The problem is, it goes to a white page. Says in the console, Trash2 is not defined. In the interview question editing itself, it doesn't give the full list of all the available types of visa based off it's parent. If it is the top, then the ANSWERS will discriminate, not the question itself, so the placement makes no sense. It should have ALL the Visa types listed for the discrimination fields that the parent question has or all if the top level and that should be for each answer, not for each question.
13) In the USCIS Forms, the delete popup has dark mode failures. The text is black on grey, so unreadable. The LLC Fee CANNOT be a percentage, so take that field out of the pricing information. The drop-downs in the Mapping and Dependencies only have the Visa categories, like F or H instead of F-1, F-2, H-1A, H-1B, and so on. Again, these are from the database, so we shouldn't be seeing this. This is completely wrong.
14) The Visa Library only has 29 Visa types, not the 88 we're supposed to have loaded and the other three identified above. This MUST be right.
15) The Attorney management page literally cannot find any of the pictures. The adding of a new attorney has dark mode text problems. I entered a name, and it's black text on the dark background. This was when I used auto-complete for it. I tried to create a new attorney, and it fails. Gives a 500 Internal Server Error when I click the create button. Why are we having negative numbers for display order? We shouldn't.
16) Reports and Analytics drops me to the home page, not showing the reports.

